[package]
name = "cerberus"
version = "1.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
indicatif = "0.18" # Для прогресс-бара
# Если используешь шифрование, добавь нужную либу здесь, например:
# aes-gcm = "0.10"